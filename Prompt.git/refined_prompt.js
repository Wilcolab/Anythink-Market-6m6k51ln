/**
 * Converts a given string to camelCase.
 *
 * This function transforms the input string into camelCase format by:
 * - Removing leading and trailing non-alphabetic characters.
 * - Trimming extra whitespace.
 * - Splitting the string into words using spaces, hyphens, underscores, or any combination as delimiters.
 * - Lowercasing the first word and capitalizing the first letter of each subsequent word.
 * - Gracefully handling edge cases such as empty strings, numbers, or null/undefined inputs by returning an empty string.
 *
 * @function toCamelCase
 * @param {string} input - The string to convert to camelCase.
 * @returns {string} The camelCase version of the input string, or an empty string if input is invalid or results in no words.
 */

/**
 * Converts a given string to dot.case.
 *
 * This function transforms the input string into dot.case format by:
 * - Removing leading and trailing non-alphanumeric characters.
 * - Trimming extra whitespace.
 * - Splitting the string into words using spaces, hyphens, underscores, or any combination as delimiters.
 * - Lowercasing all words and joining them with dots.
 * - Gracefully handling edge cases such as empty strings, numbers, or null/undefined inputs by returning an empty string.
 *
 * @function toDotCase
 * @param {string} input - The string to convert to dot.case.
 * @returns {string} The dot.case version of the input string, or an empty string if input is invalid or results in no words.
 */
